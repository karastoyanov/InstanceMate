# Copy this file to .env and fill in real values locally.
# Never commit .env or real credentials/instance data.

# --- Database ---
DATABASE_URL=postgresql://user:password@localhost:5432/instancemate

# --- Sessions / auth ---
SESSION_SECRET=changeme

# --- Embeddings ---
EMBEDDING_MODEL=local-embedding-model-name

# --- LLM providers (BYOK, set only the ones you use) ---
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# --- ServiceNow instance (per-user login is preferred; these are for local dev only) ---
SERVICENOW_INSTANCE_URL=https://your-instance.service-now.com
SERVICENOW_CLIENT_ID=
SERVICENOW_CLIENT_SECRET=

# --- MCP server ---
MCP_SERVER_URL=http://localhost:8001
